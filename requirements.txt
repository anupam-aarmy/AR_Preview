###############################
# Dependency Set (Production Content-Preserving)
# Stable versions for content-preserving generative AI
# Optimized for exact product content preservation with lighting integration
###############################

# Core AI and Vision Libraries
torch==2.3.1 ; sys_platform == 'win32' and platform_system == 'Windows'
torchvision==0.18.1 ; sys_platform == 'win32' and platform_system == 'Windows'
# (If using GPU, reinstall torch/torchvision with CUDA wheels matching your CUDA version)
opencv-python==4.10.0.84
numpy>=1.26.0,<2.0.0

# Meta's Segment Anything Model (SAM)
segment-anything>=1.0

# Content-Preserving Generative AI Pipeline - Assignment Task 2
diffusers==0.31.0            # Latest stable with ControlNet inpainting support
transformers==4.41.2         # Compatible with diffusers 0.31.0
accelerate==0.31.0           # Device / distributed utilities
huggingface_hub>=0.23.0,<0.25.0
controlnet-aux==0.0.7        # Depth / pre-processing helpers
xformers ; platform_system != 'Windows'  # Optional memory optimization
pillow>=10.2.0               # Image processing
scikit-image==0.22.0         # SSIM metrics

# Utilities for visualization and debugging
matplotlib>=3.8.0

# Additional utilities
pathlib2
requests>=2.31.0

# Notes:
# - Content-preserving pipeline preserves exact product appearance
# - Focus on lighting integration rather than content generation  
# - If installation fails for torch pinned wheels, remove version spec and install via official instructions
# - For GPU acceleration: reinstall torch/torchvision with CUDA support
# - xformers is optional; code provides safe fallback when unavailable